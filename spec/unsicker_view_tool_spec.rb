require "spec_helper"

describe UnsickerViewTool do
  it "has a version number" do
    expect(UnsickerViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
